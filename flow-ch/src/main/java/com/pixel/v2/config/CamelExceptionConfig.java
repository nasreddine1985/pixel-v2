package com.pixel.v2.config;

import org.springframework.context.annotation.Configuration;

/**
 * Exception handling is now handled by GlobalExceptionHandler This class can be removed if no
 * longer needed
 */
@Configuration
public class CamelExceptionConfig {
    // Exception handling moved to GlobalExceptionHandler class
}
