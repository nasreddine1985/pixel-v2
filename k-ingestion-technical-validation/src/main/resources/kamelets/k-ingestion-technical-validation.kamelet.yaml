apiVersion: camel.apache.org/v1alpha1
kind: Kamelet
metadata:
  name: k-ingestion-technical-validation
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "K-Ingestion Technical Validation"
    description: "Technical validation kamelet for message correctness, completeness, and adherence verification"
  template:
    from:
      uri: "kamelet:source"
      steps:
        - log: "Processing with k-ingest-validation"
        - to: "kamelet:sink"
