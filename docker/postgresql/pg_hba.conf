# PostgreSQL Client Authentication Configuration File for PIXEL-V2
# This file controls: which hosts are allowed to connect, how clients
# are authenticated, which PostgreSQL user names they can use, which
# databases they can access.

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            md5
host    all             all             0.0.0.0/0               md5

# IPv6 local connections:
host    all             all             ::1/128                 md5

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5

# PIXEL-V2 Application specific connections
host    pixelv2         pixelv2         0.0.0.0/0               md5
host    pixelv2         pixelv2         172.16.0.0/12           md5
host    pixelv2         pixelv2         192.168.0.0/16          md5
host    pixelv2         pixelv2         10.0.0.0/8              md5